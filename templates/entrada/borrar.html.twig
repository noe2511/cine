<form method="post" action="{{ path('entrada_borrar', {'peliculaIdpelicula': entrada.peliculaIdpelicula}) }}" onsubmit="return confirm('¿Esta seguro que quiere borrar esta reserva?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrada.peliculaIdpelicula) }}">
    <button class="btn">Borrar</button>
</form>
