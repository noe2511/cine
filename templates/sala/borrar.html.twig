<form method="post" action="{{ path('sala_borrar', {'idsala': sala.idsala}) }}" onsubmit="return confirm('¿Seguro que quiere borrar esta sala?');">
    <input type="hidden" name="_method" value="BORRAR">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sala.idsala) }}">
    <button class="btn">Borrar</button>
</form>
