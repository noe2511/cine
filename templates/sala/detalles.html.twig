{% extends 'base.html.twig' %}

{% block title %}Sala{% endblock %}

{% block body %}
    <h1>Sala</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id sala</th>
                <td>{{ sala.idsala }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sala_index') }}">back to list</a>

    <a href="{{ path('sala_editar', {'idsala': sala.idsala}) }}">Editar</a>

    {{ include('sala/borrar.html.twig') }}
{% endblock %}
